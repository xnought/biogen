[project]
name = "biogen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch==2.2.0",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
