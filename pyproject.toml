[project]
name = "biogen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "datasets>=3.4.0",
    "numpy<2",
    "torch==2.2.0",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
